# Optional handoff

Codex manages conversation compaction. For a requested durable handoff, save a
project note with the goal, decisions, changed paths, evidence scope, and remaining
work. Reference existing artifacts. This shared file is a template, not session state.
