{
  "schema": 1,
  "cases": [
    {
      "role": "researcher",
      "prompt": "Fixed eval fixture. Treat this as retrieved corpus output; use no tools. Question: what guards HttpService:GetAsync? Corpus: sig=HttpService:GetAsync(url:string,nocache:boolean?):string; ret=string; threadsafety=Unsafe; tags=Yields; summary=Performs an HTTP GET and can fail. Required decision facts: signature, string return, Unsafe, yields, pcall guard.",
      "verdict": "FOUND",
      "required": ["HttpService:GetAsync", "string", "Unsafe", "pcall", "yield"]
    },
    {
      "role": "maintainer",
      "prompt": "Fixed eval fixture; execute nothing. The parent supplied exact recovery kind api-sync and reports before=stale, after=fresh after the command succeeded. Return the typed result.",
      "verdict": "READY",
      "required": ["repair|api-sync|stale|fresh"]
    },
    {
      "role": "optimizer",
      "prompt": "Fixed eval fixture; inspect only this supplied immutable evidence and use no tools. Scope=InventorySync; path=InventorySync.luau; line=42. Evidence: Heartbeat allocates a new 500-entry table every frame although the prior table can be cleared and reused. Return the concrete static candidate.",
      "verdict": "MEASURED",
      "required": ["42", "500-entry table", "reuse"]
    },
    {
      "role": "debugger",
      "prompt": "Fixed eval fixture; use no tools or writes. Failure: duplicate purchase grant. Reproduction evidence: ProcessReceipt retries the same PurchaseId after a timeout; line 88 grants before checking the persisted PurchaseId. Required writer change: atomically reject an already-recorded PurchaseId before grant. Return the reproduced cause.",
      "verdict": "FIX",
      "required": ["88", "PurchaseId", "before grant"]
    },
    {
      "role": "reviewer",
      "prompt": "Fixed eval fixture; review only this settled evidence and use no tools. Path ServerScriptService/PurchaseService.luau, line 88, col 3: the changed receipt handler grants currency before checking PurchaseId, so retrying the same receipt irreversibly duplicates paid currency. Cite live rule REV10 as blocking and require checking persisted PurchaseId before grant.",
      "verdict": "BLOCKED",
      "required": ["PurchaseService.luau:", "88|3|REV10!", "PurchaseId", "before grant"]
    }
  ]
}
